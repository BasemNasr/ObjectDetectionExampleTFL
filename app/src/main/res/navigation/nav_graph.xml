<?xml version="1.0" encoding="utf-8"?>

<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/permissions_fragment">

    <fragment
        android:id="@+id/permissions_fragment"
        android:name="org.tensorflow.lite.examples.objectdetection.fragments.PermissionsFragment"
        android:label="PermissionsFragment" >

        <action
            android:id="@+id/action_permissions_to_camera"
            app:destination="@id/camera_fragment"
            app:popUpTo="@id/permissions_fragment"
            app:popUpToInclusive="true" />

    </fragment>

    <fragment
        android:id="@+id/camera_fragment"
        android:name="org.tensorflow.lite.examples.objectdetection.fragments.CameraFragment"
        android:label="CameraFragment" >


        <action
            android:id="@+id/action_camera_to_permissions"
            app:destination="@id/permissions_fragment"
            app:popUpTo="@id/camera_fragment"
            app:popUpToInclusive="true"/>
        <action
            android:id="@+id/action_camera_fragment_to_result_fragment"
            app:destination="@id/result_fragment" >
            <argument
                android:name="query"
                app:argType="string"
                android:defaultValue="default"/>

        </action>
        <action
            android:id="@+id/action_camera_fragment_to_videoFragment"
            app:destination="@id/videoFragment" />


    </fragment>
    <fragment
        android:id="@+id/result_fragment"
        android:name="org.tensorflow.lite.examples.objectdetection.fragments.ProductResultFragment"
        android:label="CameraFragment" >

        <argument
            android:name="query"
            app:argType="string"
            android:defaultValue="default"/>

    </fragment>
    <fragment
        android:id="@+id/videoFragment"
        android:name="org.tensorflow.lite.examples.objectdetection.fragments.VideoFragment"
        android:label="CameraFragment"
        tools:layout="@layout/fragment_video">

    </fragment>
</navigation>